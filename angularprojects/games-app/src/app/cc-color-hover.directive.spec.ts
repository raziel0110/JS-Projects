import { CcColorHoverDirective } from './cc-color-hover.directive';

describe('CcColorHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CcColorHoverDirective();
    expect(directive).toBeTruthy();
  });
});
