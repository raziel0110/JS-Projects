import { CcColorDirective } from './cc-color.directive';

describe('CcColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CcColorDirective();
    expect(directive).toBeTruthy();
  });
});
